---
fixes:
  - |
    Fixed an issue with QPY deserialization via the :func:`.qpy.load` function
    of the :class:`~.StatePreparation`.
    Previously, a QPY file that contained this gate would error
    when trying to load the file.
    Fixed `#8297 <https://github.com/Qiskit/qiskit-terra/issues/8297>`.
