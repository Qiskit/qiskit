---
features:
  - |
    Allow constructing :class:`~qiskit.quantum_info.Statevector` objects from circuits, assuming
    that the qubits are initialized in :math:`|0\rangle`. This also allows to call
    ``Statevector.equiv`` with a circuit as argument.