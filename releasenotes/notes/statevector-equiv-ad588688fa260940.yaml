---
features:
  - | 
    Add support for circuits and labels (e.g. '00') passed into ``Statevector.equiv``.

fixes:
  - |
    Previously, passing a circuit that creates the same statevector yielded
    ``False``, which has been fixed.
