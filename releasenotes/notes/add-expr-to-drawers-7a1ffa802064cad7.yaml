---
features:
  - |
    Added the ability to display expressions from :class:`.Expr` in the
    :meth:`.QuantumCircuit.draw` method and the :func:`~.circuit_drawer`
    function when visualizing circuits that have control flow instructions.
