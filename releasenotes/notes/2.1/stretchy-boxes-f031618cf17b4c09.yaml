---
features_circuits:
  - |
    The ``BoxOp.duration`` attribute can now be an :class:`.expr.Expr` node with type
    :class:`~.classical.types.Duration`, just like ``Delay.duration``.
    This includes stretches.
