---
features_primitives:
  - |
    Add support to the :class:`.DataBin` class to make it serializable with
    :mod:`pickle`. This enables making the :class:`.PrimitiveJob` and
    :class:`.PrimitiveResult` class serializable with :mod:`pickle` as well.
