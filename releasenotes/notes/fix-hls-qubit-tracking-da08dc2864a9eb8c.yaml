fixes:
  - |
    Fixed a bug in the :class:`.HighLevelSynthesis` transpiler pass where qubit states
    were not updated correctly after recursively synthesizing an operation.
    In certain cases, this could result in incorrect circuits being generated by the pass.
