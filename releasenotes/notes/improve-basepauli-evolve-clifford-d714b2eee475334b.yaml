---
features:
  - |
    Conjugation of a long `PauliList` by a `Clifford` using `PauliList.evolve` will now run much faster. (Though passing `evolve` an equivalent Clifford `QuantumCircuit` may still be preferable in some cases.)
