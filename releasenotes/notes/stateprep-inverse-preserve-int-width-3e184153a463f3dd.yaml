---
fixes:
  - |
    Fixed a bug in :meth:`.StatePreparation.inverse` where 
    :class:`.StatePreparation` objects specified by an integer and an explicit 
    ``num_qubits`` argument would not preserve the number of qubits.
    Fixed `#16430 <https://github.com/Qiskit/qiskit/issues/16430>`__.
