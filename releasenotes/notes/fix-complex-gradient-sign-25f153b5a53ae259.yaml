---
fixes:
  - |
    Fixed the documentation to include the sign of a gradient calculated in
    :class:`~qiskit.algorithms.gradients.LinCombEstimatorGradient` for the case of ``IMAG``
    from :class:`~qiskit.algorithms.gradients.DerivativeType`. The gradient
    had an undocumented minus sign.

