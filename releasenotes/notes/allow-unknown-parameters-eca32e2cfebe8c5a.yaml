---
features:
  - |
    Added `allow_unknown_parameters` argument to
    :meth:`~.ParameterExpression.bind` to allow passing a dictionary
    containing unknown parameters without causing an error to be raised.