---
features:
  - |
    Added ability to compute the expectation value of a :class:`.SparseObservable`
    from sampled bitstrings in the Z-basis by using the 
    :func:`.sampled_expectation_value` function.
