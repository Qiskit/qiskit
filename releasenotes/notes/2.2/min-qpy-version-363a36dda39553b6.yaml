---
features_qpy:
  - |
    Added a setting named ``min_qpy_version`` in the user config file. When set, it defines the minimum allowed QPY version
    For :func:`.qpy.load`. If the format version of a QPY file is lower than the ``minimum_qpy_version``
    setting, an exception will be raised.
