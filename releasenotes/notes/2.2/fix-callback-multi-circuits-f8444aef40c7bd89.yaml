fixes:
  - |
    Fixed a bug in :meth:`.PassManager.run` where the ``callback`` function was not
    invoked when running with multiple circuits. The callback is now correctly
    triggered for each pass on each circuit, including in parallel execution.
