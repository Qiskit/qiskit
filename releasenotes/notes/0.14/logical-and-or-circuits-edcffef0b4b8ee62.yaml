---
features:
  - |
    The circuit library provides a boolean logic AND circuit and OR circuit,
    which implement the respective operations on a number of provided
    variable qubits.
deprecations:
  - |
    The ``QuantumCircuit.AND`` and ``QuantumCircuit.OR`` method are deprecated
    in favor of the circuit library objects that can be appended to the circuit.