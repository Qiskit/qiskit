---
fixes:
  - |
    Fixed missing :attr:`.~GenericPass.property_set` in the :class:`.GenericPass` base class.
  - |
    Fixed the bug that :class:`.DoWhileController` iterates over
    the tasks of :class:`.GenericPass` subclass instance without executing them.
  - |
    Fixed the bug that :class:`.BasePassManager` accidentally splits the input value at run
    when the pass manager input is a python string.
