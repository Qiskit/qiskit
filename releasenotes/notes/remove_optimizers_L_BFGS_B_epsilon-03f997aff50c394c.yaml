---
upgrade:
  - |
    Argument parameters `epsilon` and `factr` of `~qiskit.algorithms.optimizers.L_BFGS_B`
    and `factr` of `~qiskit.algorithms.optimizers.P_BFGS` has been removed and no longer
    exists as per the deprecation notice from qiskit-terra
    0.18.0 (released XXXX, 2021).
