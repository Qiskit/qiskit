---
fixes:
  - |
    Fixed a bug in :class:`.HighLevelSynthesis`, where custom gate calibrations were
    not recognized if the gate appeared nested inside other instruction objects. 
    Partially fixed `#13728 <https://github.com/Qiskit/qiskit/issues/13728>`__.
