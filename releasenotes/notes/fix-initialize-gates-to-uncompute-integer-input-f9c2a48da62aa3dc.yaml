---
fixes:
  - |
    Fixed :meth:`.Initialize.gates_to_uncompute` to return a circuit with the
    correct number of qubits, when the gate was constructed using an integer
    form (e.g. ``Initialize(1, num_qubits=2)``).
