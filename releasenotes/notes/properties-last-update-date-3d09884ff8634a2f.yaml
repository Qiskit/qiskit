---
features:
  - |
    The `last_update_date` parameter for
    :class:`~qiskit.providers.models.BackendProperties` can now be either a
    ``datetime`` object or a datetime string (in ISO format).
upgrade:
  - |
    A new package, `python-dateutil`, is now required and has been added to
    the requirements list.
