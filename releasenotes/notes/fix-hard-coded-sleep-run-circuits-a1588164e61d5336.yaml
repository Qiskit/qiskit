---
fixes:
  - |
    The :class:`.QuantumInstance` class used by many algorithms (like VQE)
    was hard-coding the value for a sleep when it looped waiting for the job status to be updated.
    It now will respect the configured sleep value set by the ``wait`` argument to the
    class initializer.
