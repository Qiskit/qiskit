fixes:
  - |
    The eval_observables() raised an error when the quantum_circuit is a 
    StateFn. This comes from the fact that creating a StateFn from a StateFn
    results in an OperatorStateFn. A check was added to avoid this error.
    A test case was also added to assert that the error was indeed fixed.