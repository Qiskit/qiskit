---
deprecations:
  - |
    The class ``LinearFunctionsSynthesis`` was deprecated, as it is simply a special
    case of the more general ``HighLevelSynthesis`` transpiler pass. It will be
    removed in a future release.
