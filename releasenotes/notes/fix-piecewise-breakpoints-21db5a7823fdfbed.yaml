---
features:
  - |
    For ``n`` intervals allow ``n+1`` breakpoints in ``PiecewisePolynomialPauliRotations``
    to specify another end interval than``2 ^ num_qubits``. From the end of the last 
    interval to ``2 ^ num_qubits`` the function is the identity. 
