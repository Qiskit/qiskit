---
features_c:
  - |
    Added the function :c:func:`qk_target_entry_set_name` to set the name of an existing target entry.

upgrade_c:
  - |
    :c:func:`qk_target_entry_new_fixed` has an additional ``name`` parameter, to set the name of the target entry.
    Fixed-angle entries almost invariably require overriding the name of the standard gate to function correctly.
