---
features_quantum_info:
  - |
    Added :meth:`.Statevector.from_circuit` as a mirror of :meth:`.Operator.from_circuit`.
    This allows directly instantiating a :class:`.Statevector` in the space of virtual qubits,
    even for circuits that have been transpiled to a physical-qubit space.
