---
features:
  - |
    ``Statevector`` and ``DensityMatrix`` classes now have LaTeX representations
    for viewing in Jupyter Notebooks and other environments that support
    it.
  - |
    Can now use ``array_to_latex()`` to represent vectors and matrices as 
    human-friendly LaTeX.
