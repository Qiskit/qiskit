---
upgrade:
  - |
    Use the new interface `~qiskit.visualization.pulse_drawer` for pulse visualization.
deprecations:
  - |
    The classes implemented in `~qiskit.visualization.pulse` are deprecated since 0.23.
    The `pulse` folder is removed.

