---
features_c:
  - |
    Added ``qk_dag_copy_empty_like`` to create a copy of the given DAG,
    with the same structure:

    * name and other metadata
    * global phase
    * duration
    * all the qubits and clbits, including the registers.

    but without any operations.
