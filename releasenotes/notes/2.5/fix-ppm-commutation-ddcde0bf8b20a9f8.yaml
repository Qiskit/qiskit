---
fixes:
  - |
    Fixed commutativity checks between two :class:`.PauliProductMeasurement` instructions
    that measure to the same classical bit, which previously did not respect the observable write order.
