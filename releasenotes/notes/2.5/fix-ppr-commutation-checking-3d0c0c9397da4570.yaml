---
features_transpiler:
  - |
    Enabled matrix-based checks between :class:`.PauliProductRotationGate` and
    other matrix-based gates in the :class:`.CommutationChecker`.  
    Previously, these checks always returned ``False``.
