---
features_transpiler:
  - |
    Enabled matrix-based checks for :class:`.PauliProductRotationGate` in the
    :class:`.CommutationChecker`.  Previously, these checks always returned
    ``False``.
