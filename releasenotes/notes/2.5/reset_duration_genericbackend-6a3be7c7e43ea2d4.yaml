---
fixes:
  - |
    Circuits with reset instruction now can be scheduled using :class:`.GenericBackendV2`. 
    This previously failed since the :class:`.GenericBackendV2` did not have a duration for
    :class:`.Reset` operations.
