---
features_transpiler:
  - The :class:`.CommutativeCancellation` pass will now emit an :class:`.XGate` or :class:`.SXGate`
    if the tracked X rotation is a suitable multiple of :math:`\pi/2`, and the corresponding gate
    is either in the :class:`.Target` or incoming circuit.
