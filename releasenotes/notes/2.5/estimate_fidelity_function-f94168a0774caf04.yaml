---
features_c:
  - Added :c:func:`qk_circuit_estimate_fidelity`,
    which is used to estimate the fidelity of running a circuit on a
    given :c:type:`QkTarget`.

features_circuits:
  - Added :meth:`.QuantumCircuit.estimate_fidelity`, which is used
    to estimate the fidelity of running a circuit on a given
    :class:`.Target`.
