---
fixes:
  - |
    Fix the decomposition of the gates :class:`.MCXGate` and :class:`.MCPhaseGate`,
    so that the number of :class:`.CXGate` will grow quadratically in the
    number of qubits and not exponentially.
