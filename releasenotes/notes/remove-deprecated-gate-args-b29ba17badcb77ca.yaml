---
deprecations:
  - |
    The deprecated keyword arguments for the circuit gates ``q``,
    ``ctl*`` and ``tgt*`` have been removed. Instead, only the names
    ``qubit``, ``control_qubit*`` and ``target_qubit*`` can be used as named
    arguments.