---
# Choose only the "features" sections you need and delete the rest.  Try to be concise.  You may
# use a code example for complicated features.
features_c:
  - Added a "fused" operator addition with scalar multiplication to the :c:struct:`QkObs`
    interace, :c:func:`qk_obs_addmul` and :c:func:`qk_obs_iaddmul`.
