---
fixes:
  - |
    Fixed :func:`.qasm3.dump` and :func:`~.qasm3.dumps` incorrectly outputting a ``us`` suffix
    for delays specified in ``ms``.
  - |
    Fixed the conversion to ``ns`` in :func:`.qasm3.dump` and :func:`~.qasm3.dumps`
    when outputting delays specified in ``ps``.
