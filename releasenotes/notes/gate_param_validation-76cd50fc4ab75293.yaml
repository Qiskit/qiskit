---
deprecations:
  - |
    The use of `numpy.ndarray` as a parameter type for the class
    :class:`~qiskit.circuit.gate.Gate` is being deprecated.
  - |
    The class :class:`~qiskit.circuit.instruction.Instruction` should be consider a abstract
    and it should not be instantiated directly.
