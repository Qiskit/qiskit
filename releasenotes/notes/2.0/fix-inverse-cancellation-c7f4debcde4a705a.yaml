fixes:
  - |
    The :class:`.InverseCancellation` transpilation pass now runs inside of control-flow blocks. 
    Previously, it ignored  pairs of gates that could be cancelled when they were defined within classical blocks. 
    Refer to `#13437 <https://github.com/Qiskit/qiskit/issues/13437>`__ 
    for more details.
