features:
  - |
    Adds the public method :meth:`.NumpyEigensolver.eval_transition_amplitude` to
    the :class:`.NumpyEigensolver`. This allows the user to specify auxiliary operators
    and pairs of eigenstates (k,l) to evaluate the associated transition amplitudes.

