---
features:
  - |
    The objects :class:`~qiskit.circuit.Register` and 
    :class:`~qiskit.circuit.QuantumCircuit` now require the number of qubits
    to be approximately (to 1e-5) integers. Non-integer floats now raise
    errors.