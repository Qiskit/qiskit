---
fixes:
  - |
    Fixed the initialization of the :class:`.Clifford`, :class:`.Pauli` and
    :class:`.CNOTDihedral` operators from circuits that contain
    :class:`~qiskit.circuit.Delay` instructions. These instructions are treated
    as identity when converting to operators.
