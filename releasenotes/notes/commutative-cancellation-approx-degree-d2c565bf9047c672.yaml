---
features_transpiler:
  - |
    Exposed ``approximation_degree`` as argument in the
    :class:`.CommutativeCancellation` pass, which allows to set the
    approximation used when analyzing commutations.
