---
fixes:
  - |
    Fix bug in PiecewiseChebyshev so that the breakpoints parameter can be set by the user to None, therefore switching back to the default behaviour of approximating over the full interval.
