---
fixes:
  - |
    The constructor :class:`.GenericBackendV2` was allowing to create malformed backends because 
    it accepted basis gates that could not be allocated in the backend size. For example, a backend 
    with a single qubit should not accept a basis with two-qubit gates.
