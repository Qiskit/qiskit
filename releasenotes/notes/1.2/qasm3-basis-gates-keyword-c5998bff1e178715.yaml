---
fixes:
  - |
    The OpenQASM 3 exporter (see :mod:`qiskit.qasm3`) will now correctly error when asked to use a 
    keyword or other invalid identifier as a "basis gate", as it has no way of generating correct 
    output in these cases.
