---
features:
  - |
    Added :meth:`.SparsePauliOp.sum` as a specialized version of the builtin
    function ``sum`` for a list of :class:`~qiskit.quantum_info.SparsePauliOp`.
    This method has better scaling than adding the instances together in a loop.
