---
fixes:
  - |
    Sympy 1.9 is deprecating the use of ``expr_free_symbols``. The circuit drawers made
    use of this method when finding instances of constants, via
    ``pi_check()``. The deprecated call was rewritten and the
    logic around it was simplified.
