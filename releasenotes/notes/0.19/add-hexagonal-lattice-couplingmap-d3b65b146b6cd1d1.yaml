---
features:
  - |
    Added a new coupling-map constructor method :meth:`.CouplingMap.from_hexagonal_lattice`.  For example:

    .. jupyter-execute::

      from qiskit.transpiler import CouplingMap
      cmap = CouplingMap.from_hexagonal_lattice(2, 2)
      cmap.draw()
