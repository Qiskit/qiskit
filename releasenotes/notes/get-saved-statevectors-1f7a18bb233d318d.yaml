features:
  - |
    The ``result.get_statevector()`` method now has an optional ``label``
    argument that can be used to retrieve labelled statevectors saved at 
    circuit midpoints by the ``QuantumCircuit.save_statevector()`` method.