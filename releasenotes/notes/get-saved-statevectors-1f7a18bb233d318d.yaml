features:
  - |
    The ``get_statevector()`` method in ``qiskit.result.Result`` now has an 
    optional ``label`` argument that can be used to retrieve labeled 
    statevectors saved at circuit midpoints by the ``save_statevector()``
    method in ``qiskit.circuit.QuantumCircuit``.