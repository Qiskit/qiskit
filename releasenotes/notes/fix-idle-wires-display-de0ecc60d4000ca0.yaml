---
fixes:
  - |
    A bug in the ``mpl`` and ``latex`` circuit drawers, when
    setting the ``idle_wires`` option to False when there was
    a ``barrier`` in the circuit would cause the drawers to
    fail, has been fixed.
