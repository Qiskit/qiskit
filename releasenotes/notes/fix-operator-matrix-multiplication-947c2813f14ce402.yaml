---
fixes:
  - |
    The :meth:`.Operator.compose` method now correctly handles
    :class:`.Statevector` inputs by delegating to
    :meth:`.Statevector.evolve`, ensuring consistent behavior with
    operator–statevector application.