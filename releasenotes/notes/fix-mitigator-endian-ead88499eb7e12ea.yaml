---
fixes:
  - |
    Fixed the bug in :meth:`~qiskit.result.BaseReadoutMitigator.expectation_value`
    where the endian was reversed when a string `diagonal` is passed.
    It used to be interpreted as big endian, but now it will be interpreted as little endian,
    which is the default for Qiskit.
