---
features:
  - |
    Added rules for converting to :class:`.XXPlusYYGate` and
    :class:`.XXMinusYYGate` to the ``SessionEquivalenceLibrary``. This enables
    running :func:`~.transpile` targetting a backend or :class:`~.Target` that
    uses these gates.
