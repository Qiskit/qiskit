---
deprecations_providers:
  - |
    The :class:`.BackendV2Converter` function has been deprecated in Qiskit v1.4.0,
    as a consequence of the deprecation of :class:`.BackendV1`. This tool 
    is used for converting :class:`.BackendV1` to :class:`.BackendV2`, and cannot 
    be maintained once :class:`.BackendV1` is removed.