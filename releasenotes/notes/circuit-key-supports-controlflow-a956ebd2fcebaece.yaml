---
features:
  - |
    Primitives support the dynamic circuits with control flow.
    This improvement is applied to all :class:`~BaseSampler` and :class:`~BaseEstimator` subclasses,
    if the backend support running the dynamic circuits.
